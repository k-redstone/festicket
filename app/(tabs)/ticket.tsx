import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TicketScreen() {
  return (
    <SafeAreaView>
      <View>
        <Text className=" text-gray-300">TicketScreen</Text>
      </View>
    </SafeAreaView>
  );
}
